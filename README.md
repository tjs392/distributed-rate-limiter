# Distributed Rate Limiter

A gossip-based distributed rate limiter in Rust using CRDTs, sliding window counters, and UDP gossip protocol. No external dependencies (no Redis, no etcd). Single binary, self-clustering.

> **Note:** Tests are currently AI generated for quicker iteration. Hand written tests with better coverage and edge cases are ocming.